{
  "name": "usefull-utils",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "Alexej Bronshtein",
  "license": "ISC",
  "scripts": {
    "build:functions": "cd ./functions && npm run build && npm run go:publish",
    "build:node": "cd ./node && npm run build && npm run go:publish",
    "build:storage": "cd ./storage && npm run build && npm run go:publish"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "ts-babel-node": "^1.1.1",
    "ts-node": "^10.9.1",
    "tsc": "^2.0.4",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "@zoppa/functions": "^1.1.1",
    "@zoppa/node": "^1.0.2",
    "copyfiles": "^2.4.1"
  }
}
